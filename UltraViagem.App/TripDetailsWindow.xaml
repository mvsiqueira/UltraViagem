<Window x:Class="UltraViagem.App.TripDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Viagem"
        Width="520"
        Height="520"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource PageBackground}">
    <Border Style="{StaticResource Card}" Margin="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock x:Name="TitleText" Text="Viagem" FontSize="24" FontWeight="Bold" />
                <TextBlock Text="Dados gerais salvos no trip.json da pasta da viagem." Foreground="{StaticResource MutedTextBrush}" Margin="0,4,0,18" />
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="130" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel Grid.ColumnSpan="2">
                    <TextBlock Text="Nome" Style="{StaticResource Caption}" />
                    <TextBox x:Name="TripTitleBox" Padding="10" FontSize="16" Margin="0,4,0,14" />
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,0,12,0">
                    <TextBlock Text="Data inicial (aaaa-mm-dd)" Style="{StaticResource Caption}" />
                    <TextBox x:Name="StartDateBox" Padding="10" Margin="0,4,0,14" />
                </StackPanel>

                <StackPanel Grid.Row="1" Grid.Column="1">
                    <TextBlock Text="Pessoas" Style="{StaticResource Caption}" />
                    <TextBox x:Name="PeopleBox" Padding="10" Margin="0,4,0,14" />
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="0,0,12,0">
                    <TextBlock Text="Data final (aaaa-mm-dd)" Style="{StaticResource Caption}" />
                    <TextBox x:Name="EndDateBox" Padding="10" Margin="0,4,0,14" />
                </StackPanel>

                <StackPanel Grid.Row="2" Grid.Column="1">
                    <TextBlock Text="Moeda" Style="{StaticResource Caption}" />
                    <TextBox x:Name="CurrencyBox" Padding="10" Margin="0,4,0,14" />
                </StackPanel>

                <StackPanel Grid.Row="3" Grid.ColumnSpan="2">
                    <TextBlock Text="Link do Google My Maps" Style="{StaticResource Caption}" />
                    <TextBox x:Name="MyMapsBox" Padding="10" Margin="0,4,0,14" />
                </StackPanel>

                <TextBlock Grid.Row="4"
                           x:Name="ErrorText"
                           Foreground="#B42318"
                           TextWrapping="Wrap"
                           Visibility="Collapsed" />
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancelar" Style="{StaticResource SecondaryButton}" Click="Cancel_Click" Margin="0,0,10,0" />
                <Button Content="Salvar" Style="{StaticResource PrimaryButton}" Click="Save_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
