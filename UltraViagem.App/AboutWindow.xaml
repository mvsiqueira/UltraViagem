<Window x:Class="UltraViagem.App.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sobre o UltraViagem"
        Width="500"
        Height="360"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource PageBackground}">
    <Border Style="{StaticResource Card}" Margin="18">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,18">
                <Border Width="54" Height="54" Background="{StaticResource AccentSoftBrush}" CornerRadius="27" Margin="0,0,14,0">
                    <TextBlock Text="&#xE709;"
                               FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                               FontSize="26"
                               Foreground="{StaticResource AccentBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center" />
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="UltraViagem" FontSize="25" FontWeight="Bold" />
                    <TextBlock x:Name="VersionText" Foreground="{StaticResource MutedTextBrush}" Margin="0,3,0,0" />
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="1">
                <TextBlock Text="UltraViagem é um organizador pessoal para planejar viagens, reunindo tarefas, dicas, arquivos anexos e visualização de mapas em um único lugar."
                           TextWrapping="Wrap"
                           FontSize="14"
                           LineHeight="20"
                           Margin="0,0,0,18" />

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Text="Autor" Style="{StaticResource Caption}" Margin="0,0,0,10" />
                    <TextBlock Grid.Column="1" Text="Marcus Siqueira via ChatGPT Codex" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="1" Text="Build" Style="{StaticResource Caption}" />
                    <TextBlock Grid.Row="1" Grid.Column="1" x:Name="BuildDateText" />
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="OK"
                        Style="{StaticResource PrimaryButton}"
                        MinWidth="92"
                        Click="Ok_Click" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
