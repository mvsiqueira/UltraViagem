<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:core="clr-namespace:UltraViagem.Core;assembly=UltraViagem.Core"
             x:Class="UltraViagem.Android.Pages.TasksPage"
             Title="Tarefas">

    <CollectionView ItemsSource="{Binding Trip.Tasks}"
                    Margin="16,8"
                    SelectionMode="None">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="core:TaskItem">
                <Grid ColumnDefinitions="32,*" ColumnSpacing="10" Padding="0,8">

                    <!-- Checkbox visual (não interativo) -->
                    <Frame Grid.Column="0"
                           WidthRequest="22" HeightRequest="22"
                           CornerRadius="4" Padding="0"
                           BorderColor="{StaticResource Border}"
                           HorizontalOptions="Center" VerticalOptions="Center">
                        <Frame.Triggers>
                            <DataTrigger TargetType="Frame"
                                         Binding="{Binding Status}" Value="done">
                                <Setter Property="BackgroundColor" Value="{StaticResource Accent}" />
                                <Setter Property="BorderColor" Value="{StaticResource Accent}" />
                            </DataTrigger>
                        </Frame.Triggers>
                        <Label Text="✓" FontSize="12" FontAttributes="Bold"
                               TextColor="White"
                               HorizontalOptions="Center" VerticalOptions="Center">
                            <Label.Triggers>
                                <DataTrigger TargetType="Label"
                                             Binding="{Binding Status}" Value="pending">
                                    <Setter Property="IsVisible" Value="False" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                    </Frame>

                    <!-- Texto -->
                    <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="2">
                        <Label Text="{Binding Title}" FontSize="14">
                            <Label.Triggers>
                                <DataTrigger TargetType="Label"
                                             Binding="{Binding Status}" Value="done">
                                    <Setter Property="TextDecorations" Value="Strikethrough" />
                                    <Setter Property="TextColor" Value="{StaticResource TextMuted}" />
                                </DataTrigger>
                                <DataTrigger TargetType="Label"
                                             Binding="{Binding Status}" Value="pending">
                                    <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
                                </DataTrigger>
                            </Label.Triggers>
                        </Label>
                        <Label Text="{Binding Notes}"
                               Style="{StaticResource MutedLabel}" FontSize="12"
                               IsVisible="{Binding Notes, Converter={x:StaticResource StringToBool}}" />
                    </VerticalStackLayout>

                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
        <CollectionView.EmptyView>
            <Label Text="Nenhuma tarefa registrada"
                   Style="{StaticResource MutedLabel}"
                   HorizontalOptions="Center" Margin="0,40,0,0" />
        </CollectionView.EmptyView>
    </CollectionView>
</ContentPage>
